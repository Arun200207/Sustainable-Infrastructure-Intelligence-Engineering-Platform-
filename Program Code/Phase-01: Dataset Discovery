import os
import numpy as np
import pandas as pd

# Ensure DATA_PATH is defined in your environment or configuration
# DATA_PATH = "path/to/your/data"

files = os.listdir(DATA_PATH)
csv_files = [f for f in files if f.endswith(".csv")]

def profile_dataset(path):
    """Reads the first 5 rows of a CSV file and returns basic structural metadata."""
    df = pd.read_csv(path, nrows=5)
    return {
        "file": os.path.basename(path),
        "columns": len(df.columns),
        "sample_columns": list(df.columns[:10])
    }

profiles = []
for file in csv_files:
    file_path = os.path.join(DATA_PATH, file)
    profiles.append(profile_dataset(file_path))

profile_df = pd.DataFrame(profiles)
